# M4.2B time-void source

`time_void_tile_alpha.png` is the selected 1024×1024 green chroma-key source. The runtime export is a transparent 64×64 single-cell tile at `game/assets/environment/time_void_tile.png`.

The tile never overflows its logical cell. It is rendered below tactical overlays so movement, enemy intent, ghost fire, and knockback previews keep the D-026 information priority.
