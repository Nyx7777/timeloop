# 项目协作约定

## 对话记忆

- 每次开始设计或开发前，优先阅读 `时间循环玩法/项目记忆_新对话接续.md`。
- 每完成一轮有实质进展的设计或开发，都要更新上述记忆文件。
- 记忆应记录已经确认的结论、关键理由、当前实现状态、未解决问题和明确的下一步，不要逐字复制聊天记录。
- 详细设计决策同步维护在 `时间循环玩法/项目TODO与设计决策.md`；整体规则变化同步维护在 `时间循环玩法/设计框架v2_时间循环.md`。
- 文档发生冲突时，以日期较新的明确决策为准，并在同一轮工作中修正文档冲突。

## GitHub 同步

- 默认分支为 `main`，远端为 `origin`。
- 每轮开发完成并验证后，先更新项目记忆，再提交本轮相关改动并推送至 `origin/main`。
- 提交信息应简洁说明本轮实际成果；不要提交密钥、登录凭据、临时文件或明显损坏的中间状态。
- 如果推送前发现远端新增提交、合并冲突、测试失败或疑似敏感信息，暂停推送并向用户说明，不要强制覆盖远端。
- 用户明确要求暂不提交或暂不同步时，以用户当次要求为准。
